docker run/docker stop
docker pull - doesn't run a container with the image
docker ps -a
docker rm id or name
docker rmi image (need to remove containers first)
docker images

on a running container:
docker exe container-id "command" (like: 'cat /etc/*release*')

can test them with the hello_world image
